const notEmpty = ' cannot be empty'
const emailNotValid = 'Email is not a valid format' 
const emailNotUnique = 'Email has already been registered'
const passwordLength = 'Password needs to be between 8 and 64 characters'

module.exports = {notEmpty, emailNotValid, passwordLength, emailNotUnique}